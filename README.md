# spam-email-classifier

A machine learning project that classifies emails as spam or non-spam using a Kaggle dataset. 
The model is built using Multinomial Naive Bayes and achieves high accuracy with optimized performance.
